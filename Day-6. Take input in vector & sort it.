#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    vector<int> v;
    int n;
    cin >> n;
    //taking inputs
     for(int i = 0; i<n; i++){
        int x;
        cin>>x;
        v.push_back(x);
    }
    //using sort inbuilt function
    //sorted using inbuilt sort function
    sort(v.begin(),v.end());
    //outputing sorted values
    for(int i = 0; i<v.size(); i++){
        cout << v[i] << " ";
    }
    return 0;
}
